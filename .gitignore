# build outputs (contain APKs and on-device screenshots)
build/
shots/
*.apk
*.dex

# screenshots / previews that may contain personal photos
*_preview.png
shots/

# signing key — never commit
*.keystore

# misc
.gradle/
.DS_Store

# screenshot scratch (generic demo photos)
ghshots/
vshots/
oshots/
gridshots/
